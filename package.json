{
  "name": "demoguesser",
  "version": "1.0.0",
  "description": "A webapp-game",
  "main": "index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "drun": "nodemon index.js",
    "start": "node index.js",
    "heroku-postbuild": "cd Client && npm install --only=dev && npm install && npm run build"
  },
  "author": "",
  "license": "ISC"
}
